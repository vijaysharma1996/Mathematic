
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int arr[7]={-6,3,1,-4,4,6,5};
    unordered_map<int,bool> mymap;
    for(int i=0;i<7;i++)
    {
    
         mymap.insert({ arr[i],true});
    }
    
//     for (auto x : mymap) {
// cout << x.first << " " << x.second << "\n";
// }
 
   for(int i=0;i<7;i++)
    {
        if(arr[i]>0)
        {
    // If key not found in map iterator to end is returned
    if (mymap.find(i) == mymap.end())
        cout << i;
        }
    
}
    
    

    return 0;
}
